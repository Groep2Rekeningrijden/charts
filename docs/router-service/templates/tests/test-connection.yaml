apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "router-service.fullname" . }}-test-connection"
  labels:
    {{- include "router-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "router-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
