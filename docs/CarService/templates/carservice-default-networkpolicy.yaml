apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: carservice-default
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/carservice-default: "true"
  podSelector:
    matchLabels:
      io.kompose.network/carservice-default: "true"
